diff --git a/app/admin/library/[id]/edit/page.js b/app/admin/library/[id]/edit/page.js
index 6673527..de72d0d 100644
--- a/app/admin/library/[id]/edit/page.js
+++ b/app/admin/library/[id]/edit/page.js
@@ -14,8 +14,8 @@ export default function EditBook() {
     author: '',
     description: '',
     price: 0,
-    cover_image_url: '',
-    file_url: '',
+    cover_url: '',
+    pdf_url: '',
   });
   const [coverFile, setCoverFile] = useState(null);
   const [fileFile, setFileFile] = useState(null);
@@ -55,14 +55,14 @@ export default function EditBook() {
     e.preventDefault();
     setUploading(true);
     try {
-      let coverUrl = form.cover_image_url;
-      let fileUrl = form.file_url;
+      let coverUrl = form.cover_url;
+      let fileUrl = form.pdf_url;
       if (coverFile) coverUrl = await handleUpload(coverFile, 'covers');
       if (fileFile) fileUrl = await handleUpload(fileFile, 'files');
 
       const { error } = await supabase
         .from('books')
-        .update({ ...form, cover_image_url: coverUrl, file_url: fileUrl })
+        .update({ ...form, cover_url: coverUrl, pdf_url: fileUrl })
         .eq('id', id);
       if (error) throw error;
       router.push('/admin/library');
@@ -113,11 +113,11 @@ export default function EditBook() {
         />
 
         {/* Current Cover */}
-        {form.cover_image_url && (
+        {form.cover_url && (
           <div>
             <p className="text-sm font-medium">Current Cover</p>
             <img
-              src={form.cover_image_url}
+              src={form.cover_url}
               alt="Cover"
               className="h-24 w-auto object-cover"
             />
@@ -136,11 +136,11 @@ export default function EditBook() {
         </div>
 
         {/* Current File */}
-        {form.file_url && (
+        {form.pdf_url && (
           <div>
             <p className="text-sm font-medium">Current File</p>
             <a
-              href={form.file_url}
+              href={form.pdf_url}
               target="_blank"
               className="text-blue-600 underline text-sm"
             >
diff --git a/app/admin/library/add/page.js b/app/admin/library/add/page.js
index 399acea..c013843 100644
--- a/app/admin/library/add/page.js
+++ b/app/admin/library/add/page.js
@@ -45,8 +45,9 @@ export default function AddBook() {
       const { error } = await supabase.from('books').insert([
         {
           ...form,
-          cover_image_url: coverUrl,
-          file_url: fileUrl,
+          cover_url: coverUrl,
+          pdf_url: fileUrl,
+          is_published: true,
         },
       ]);
       if (error) throw error;
diff --git a/app/api/content-engine/generate/route.js b/app/api/content-engine/generate/route.js
index ec95072..bf28cb9 100644
--- a/app/api/content-engine/generate/route.js
+++ b/app/api/content-engine/generate/route.js
@@ -8,6 +8,7 @@ import {
   createFallbackThumbnail,
   IMAGE_PRESETS,
 } from '@/lib/image-engine';
+import { parseJsonFromText as robustParseJsonFromText } from '@/lib/robustJsonParse';
 
 // ── API Keys ─────────────────────────────────────────────────────────────
 const GEMINI_KEYS = [
@@ -105,10 +106,7 @@ Return ONLY this JSON object – no markdown fences, no extra text. The "content
 
 // ─── Helpers ───────────────────────────────────────────────────────────
 function parseJsonFromText(text) {
-  const cleaned = text.replace(/```json|```/g, '').trim();
-  const match = cleaned.match(/\{[\s\S]*\}/);
-  if (!match) return null;
-  return JSON.parse(match[0]);
+  return robustParseJsonFromText(text, 'object');
 }
 
 function isValidResult(r) {
diff --git a/app/api/engines/boss-battle/route.js b/app/api/engines/boss-battle/route.js
index d7d6e85..90064d3 100644
--- a/app/api/engines/boss-battle/route.js
+++ b/app/api/engines/boss-battle/route.js
@@ -2,6 +2,7 @@ import { NextResponse } from 'next/server';
 import { createAdminClient } from '@/lib/supabase-admin';
 import { GoogleGenerativeAI } from '@google/generative-ai';
 import Groq from 'groq-sdk';
+import { parseJsonFromText } from '@/lib/robustJsonParse';
 
 // ── Keys ──────────────────────────────────────────────────────────────
 const GEMINI_KEYS = [
@@ -26,16 +27,6 @@ const GEMINI_MODELS = [
   'gemini-2.0-flash',
 ];
 
-function parseJsonFromText(text) {
-  const cleaned = text.replace(/```json|```/g, '').trim();
-  const match = cleaned.match(/\{[\s\S]*\}/);
-  if (!match) return null;
-  return JSON.parse(match[0]);
-}
-
-function sanitizeJsonString(str) {
-  return str.replace(/[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g, '');
-}
 
 async function tryOpenRouter(prompt) {
   if (!OPENROUTER_API_KEY) return null;
@@ -153,8 +144,7 @@ export async function POST(request) {
             generationConfig: { maxOutputTokens: 4096, temperature: 0.8 },
           });
           const text = genResult.response.text();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.questions) && parsed.questions.length >= 8) {
             result = parsed;
             usedProvider = `Gemini (${modelName})`;
@@ -179,8 +169,7 @@ export async function POST(request) {
             temperature: 0.8,
           });
           const text = groqResponse.choices[0].message.content.trim();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.questions) && parsed.questions.length >= 8) {
             result = parsed;
             usedProvider = `Groq (${GROQ_KEYS.indexOf(groqKey) + 1})`;
@@ -197,8 +186,7 @@ export async function POST(request) {
       try {
         const text = await tryOpenRouter(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.questions) && parsed.questions.length >= 8) {
             result = parsed;
             usedProvider = 'OpenRouter';
@@ -215,8 +203,7 @@ export async function POST(request) {
       try {
         const text = await tryHuggingFace(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.questions) && parsed.questions.length >= 8) {
             result = parsed;
             usedProvider = 'HuggingFace';
diff --git a/app/api/engines/flashcards/route.js b/app/api/engines/flashcards/route.js
index f5e1bcb..da83e65 100644
--- a/app/api/engines/flashcards/route.js
+++ b/app/api/engines/flashcards/route.js
@@ -2,6 +2,7 @@ import { NextResponse } from 'next/server';
 import { createAdminClient } from '@/lib/supabase-admin';
 import { GoogleGenerativeAI } from '@google/generative-ai';
 import Groq from 'groq-sdk';
+import { parseJsonFromText } from '@/lib/robustJsonParse';
 
 const GEMINI_KEYS = [
   process.env.GEMINI_API_KEY_1,
@@ -24,16 +25,6 @@ const GEMINI_MODELS = [
   'gemini-2.5-flash-lite',
 ];
 
-function parseJsonFromText(text) {
-  const cleaned = text.replace(/```json|```/g, '').trim();
-  const match = cleaned.match(/\{[\s\S]*\}/);
-  if (!match) return null;
-  return JSON.parse(match[0]);
-}
-
-function sanitizeJsonString(str) {
-  return str.replace(/[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g, '');
-}
 
 async function tryOpenRouter(prompt) {
   if (!OPENROUTER_API_KEY) return null;
@@ -174,8 +165,7 @@ export async function POST(request) {
             generationConfig: { maxOutputTokens: 4096, temperature: 0.7 },
           });
           const text = genResult.response.text();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.cards) && parsed.cards.length >= 15) {
             result = parsed;
             usedProvider = `Gemini (${modelName})`;
@@ -201,8 +191,7 @@ export async function POST(request) {
             temperature: 0.7,
           });
           const text = groqResponse.choices[0].message.content.trim();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.cards) && parsed.cards.length >= 15) {
             result = parsed;
             usedProvider = `Groq (${GROQ_KEYS.indexOf(groqKey) + 1})`;
@@ -220,8 +209,7 @@ export async function POST(request) {
       try {
         const text = await tryOpenRouter(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.cards) && parsed.cards.length >= 15) {
             result = parsed;
             usedProvider = 'OpenRouter';
@@ -239,8 +227,7 @@ export async function POST(request) {
       try {
         const text = await tryHuggingFace(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.cards) && parsed.cards.length >= 15) {
             result = parsed;
             usedProvider = 'HuggingFace';
diff --git a/app/api/engines/quiz/route.js b/app/api/engines/quiz/route.js
index 18ea80f..c1df50f 100644
--- a/app/api/engines/quiz/route.js
+++ b/app/api/engines/quiz/route.js
@@ -2,6 +2,7 @@ import { NextResponse } from 'next/server';
 import { createAdminClient } from '@/lib/supabase-admin';
 import { GoogleGenerativeAI } from '@google/generative-ai';
 import Groq from 'groq-sdk';
+import { parseJsonFromText } from '@/lib/robustJsonParse';
 
 // ── Keys ──────────────────────────────────────────────────────────────
 const GEMINI_KEYS = [
@@ -27,17 +28,6 @@ const GEMINI_MODELS = [
 ];
 
 // ── Helpers ──────────────────────────────────────────────────────────
-function parseJsonFromText(text) {
-  const cleaned = text.replace(/```json|```/g, '').trim();
-  const match = cleaned.match(/\{[\s\S]*\}/);
-  if (!match) return null;
-  return JSON.parse(match[0]);
-}
-
-function sanitizeJsonString(str) {
-  // Remove raw control characters (except newline and tab) that break JSON.parse
-  return str.replace(/[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g, '');
-}
 
 async function tryOpenRouter(prompt) {
   if (!OPENROUTER_API_KEY) return null;
@@ -170,8 +160,7 @@ export async function POST(request) {
             generationConfig: { maxOutputTokens: 4096, temperature: 0.7 },
           });
           const text = genResult.response.text();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.questions) && parsed.questions.length >= 10) {
             result = parsed;
             usedProvider = `Gemini (${modelName})`;
@@ -197,8 +186,7 @@ export async function POST(request) {
             temperature: 0.7,
           });
           const text = groqResponse.choices[0].message.content.trim();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.questions) && parsed.questions.length >= 10) {
             result = parsed;
             usedProvider = `Groq (${GROQ_KEYS.indexOf(groqKey) + 1})`;
@@ -216,8 +204,7 @@ export async function POST(request) {
       try {
         const text = await tryOpenRouter(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.questions) && parsed.questions.length >= 10) {
             result = parsed;
             usedProvider = 'OpenRouter';
@@ -235,8 +222,7 @@ export async function POST(request) {
       try {
         const text = await tryHuggingFace(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.questions) && parsed.questions.length >= 10) {
             result = parsed;
             usedProvider = 'HuggingFace';
diff --git a/app/api/engines/social/route.js b/app/api/engines/social/route.js
index 3ecbc87..ef40557 100644
--- a/app/api/engines/social/route.js
+++ b/app/api/engines/social/route.js
@@ -2,6 +2,7 @@ import { NextResponse } from 'next/server';
 import { createAdminClient } from '@/lib/supabase-admin';
 import { GoogleGenerativeAI } from '@google/generative-ai';
 import Groq from 'groq-sdk';
+import { parseJsonFromText } from '@/lib/robustJsonParse';
 
 // ── Keys ──────────────────────────────────────────────────────────────
 const GEMINI_KEYS = [
@@ -26,16 +27,6 @@ const GEMINI_MODELS = [
 ];
 
 // ── Helpers ──────────────────────────────────────────────────────────
-function parseJsonFromText(text) {
-  const cleaned = text.replace(/```json|```/g, '').trim();
-  const match = cleaned.match(/\{[\s\S]*\}/);
-  if (!match) return null;
-  return JSON.parse(match[0]);
-}
-
-function sanitizeJsonString(str) {
-  return str.replace(/[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g, '');
-}
 
 async function tryOpenRouter(prompt) {
   if (!OPENROUTER_API_KEY) return null;
@@ -165,8 +156,7 @@ export async function POST(request) {
             generationConfig: { maxOutputTokens: 4096, temperature: 0.8 },
           });
           const text = genResult.response.text();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.posts) && parsed.posts.length >= 4) {
             result = parsed;
             usedProvider = `Gemini (${modelName})`;
@@ -192,8 +182,7 @@ export async function POST(request) {
             temperature: 0.8,
           });
           const text = groqResponse.choices[0].message.content.trim();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.posts) && parsed.posts.length >= 4) {
             result = parsed;
             usedProvider = `Groq (${GROQ_KEYS.indexOf(groqKey) + 1})`;
@@ -211,8 +200,7 @@ export async function POST(request) {
       try {
         const text = await tryOpenRouter(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.posts) && parsed.posts.length >= 4) {
             result = parsed;
             usedProvider = 'OpenRouter';
@@ -230,8 +218,7 @@ export async function POST(request) {
       try {
         const text = await tryHuggingFace(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && Array.isArray(parsed.posts) && parsed.posts.length >= 4) {
             result = parsed;
             usedProvider = 'HuggingFace';
diff --git a/app/api/engines/study-notes/route.js b/app/api/engines/study-notes/route.js
index 3eb322e..c7812a3 100644
--- a/app/api/engines/study-notes/route.js
+++ b/app/api/engines/study-notes/route.js
@@ -2,6 +2,7 @@ import { NextResponse } from 'next/server';
 import { createAdminClient } from '@/lib/supabase-admin';
 import { GoogleGenerativeAI } from '@google/generative-ai';
 import Groq from 'groq-sdk';
+import { parseJsonFromText as robustParseJsonFromText } from '@/lib/robustJsonParse';
 
 // ── Keys ──────────────────────────────────────────────────────────────
 const GEMINI_KEYS = [
@@ -27,28 +28,13 @@ const GEMINI_MODELS = [
 
 // ── Helpers ──────────────────────────────────────────────────────────
 function parseJsonFromText(text) {
-  const cleaned = text.replace(/```json|```/g, '').trim();
-  const match = cleaned.match(/\{[\s\S]*\}/);
-  if (!match) return null;
   try {
-    return JSON.parse(match[0]);
+    return robustParseJsonFromText(text, 'object');
   } catch {
     return null;
   }
 }
 
-function sanitizeJsonString(str) {
-  if (!str) return '';
-  // Remove raw control characters (except newline and tab)
-  let cleaned = str.replace(/[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g, '');
-  // Remove Unicode control characters
-  cleaned = cleaned.replace(/[\u200B-\u200F\u2028-\u202F]/g, '');
-  // Escape raw newlines inside JSON strings (this is the key fix)
-  // We'll use a simple approach: replace raw newlines with \n
-  // but only after we've identified the JSON structure
-  return cleaned;
-}
-
 function extractTitleFromContent(content) {
   const match = content.match(/^#{1,3}\s+(.+)/m);
   return match ? match[1].trim() : null;
@@ -189,8 +175,7 @@ export async function POST(request) {
             generationConfig: { maxOutputTokens: 4096, temperature: 0.7 },
           });
           const text = genResult.response.text();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && typeof parsed.content === 'string' && parsed.content.length > 100) {
             if (!parsed.title) {
               parsed.title = extractTitleFromContent(parsed.content) || `${asset.keyword} - Notes`;
@@ -221,8 +206,7 @@ export async function POST(request) {
             temperature: 0.7,
           });
           const text = groqResponse.choices[0].message.content.trim();
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && typeof parsed.content === 'string' && parsed.content.length > 100) {
             if (!parsed.title) {
               parsed.title = extractTitleFromContent(parsed.content) || `${asset.keyword} - Notes`;
@@ -244,8 +228,7 @@ export async function POST(request) {
       try {
         const text = await tryOpenRouter(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && typeof parsed.content === 'string' && parsed.content.length > 100) {
             if (!parsed.title) {
               parsed.title = extractTitleFromContent(parsed.content) || `${asset.keyword} - Notes`;
@@ -266,8 +249,7 @@ export async function POST(request) {
       try {
         const text = await tryHuggingFace(prompt);
         if (text) {
-          const cleaned = sanitizeJsonString(text);
-          const parsed = parseJsonFromText(cleaned);
+          const parsed = parseJsonFromText(text);
           if (parsed && typeof parsed.content === 'string' && parsed.content.length > 100) {
             if (!parsed.title) {
               parsed.title = extractTitleFromContent(parsed.content) || `${asset.keyword} - Notes`;
diff --git a/app/api/engines/visual-blueprint/route.js b/app/api/engines/visual-blueprint/route.js
index c626b8e..1d2c66e 100644
--- a/app/api/engines/visual-blueprint/route.js
+++ b/app/api/engines/visual-blueprint/route.js
@@ -2,6 +2,7 @@
 import { NextResponse } from 'next/server';
 import { createAdminClient } from '@/lib/supabase-admin';
 import { generateWithFallback } from '@/lib/llmFallbackChain';
+import { parseJsonFromText as robustParseJsonFromText } from '@/lib/robustJsonParse';
 
 // Fixed vocabulary — the images route uses this to decide WHICH sources to
 // even query, so the LLM must only ever return one of these.
@@ -14,11 +15,9 @@ const VALID_TYPES = [
 ];
 
 function parseJsonFromText(text) {
-  const cleaned = text.replace(/```json|```/g, '').trim();
-  const match = cleaned.match(/\{[\s\S]*\}|\[[\s\S]*\]/);
-  if (!match) return null;
+  // The blueprint response is always a top-level array of plan items.
   try {
-    return JSON.parse(match[0]);
+    return robustParseJsonFromText(text, 'array');
   } catch {
     return null;
   }
diff --git a/app/api/learning-core/generate/route.js b/app/api/learning-core/generate/route.js
index fc32b34..741435e 100644
--- a/app/api/learning-core/generate/route.js
+++ b/app/api/learning-core/generate/route.js
@@ -2,6 +2,7 @@ import { NextResponse } from 'next/server';
 import { createRouteHandlerClient } from '@/lib/supabase-server';
 import { GoogleGenerativeAI } from '@google/generative-ai';
 import Groq from 'groq-sdk';
+import { parseJsonFromText as robustParseJsonFromText } from '@/lib/robustJsonParse';
 
 // ── Keys and providers ──────────────────────────────────────────────
 const GEMINI_KEYS = [
@@ -50,10 +51,7 @@ Return ONLY a JSON object with the following fields – no markdown, no extra te
 
 // ── Helpers ──────────────────────────────────────────────────────────
 function parseJsonFromText(text) {
-  const cleaned = text.replace(/```json|```/g, '').trim();
-  const match = cleaned.match(/\{[\s\S]*\}/);
-  if (!match) return null;
-  return JSON.parse(match[0]);
+  return robustParseJsonFromText(text, 'object');
 }
 
 function isValidKnowledge(r) {
diff --git a/app/api/study-notes/[draftId]/publish/route.js b/app/api/study-notes/[draftId]/publish/route.js
index af46bd3..2d7c832 100644
--- a/app/api/study-notes/[draftId]/publish/route.js
+++ b/app/api/study-notes/[draftId]/publish/route.js
@@ -53,7 +53,7 @@ export async function POST(request, { params }) {
         .update({
           title,
           description: draft.summary || `Exam-ready study notes on ${keyword}`,
-          file_url: urlData.publicUrl,
+          pdf_url: urlData.publicUrl,
           is_published: true,
         })
         .eq('id', bookId);
@@ -68,7 +68,7 @@ export async function POST(request, { params }) {
           author: 'Shiney Brain Academy',
           description: draft.summary || `Exam-ready study notes on ${keyword}`,
           price: 0,
-          file_url: urlData.publicUrl,
+          pdf_url: urlData.publicUrl,
           is_published: true,
         })
         .select()
diff --git a/lib/pdf/CoverPage.js b/lib/pdf/CoverPage.js
index 5bac5c2..edce46e 100644
--- a/lib/pdf/CoverPage.js
+++ b/lib/pdf/CoverPage.js
@@ -1,17 +1,50 @@
 // lib/pdf/CoverPage.js
 import React from 'react';
-import { Page, View, Text, StyleSheet } from '@react-pdf/renderer';
+import path from 'path';
+import { Page, View, Text, Image, StyleSheet } from '@react-pdf/renderer';
 import brand from './brand';
 import Header from './Header';
 import Footer from './Footer';
 
+const LOGO_PATH = path.join(process.cwd(), 'lib', 'logo.jpg');
+
 const styles = StyleSheet.create({
   coverTitle: { fontSize: 26, fontFamily: 'Helvetica-Bold', color: brand.colors.primary, textAlign: 'center', marginTop: 200 },
   coverSubtitle: { fontSize: 13, color: '#555', textAlign: 'center', marginTop: 10 },
   coverBrand: { fontSize: 14, color: brand.colors.secondary, textAlign: 'center', marginTop: 260, fontFamily: 'Helvetica-Bold' },
-  backPage: { paddingTop: 70, paddingBottom: 60, paddingHorizontal: 40, fontFamily: 'Helvetica' },
-  backPageTitle: { fontSize: 18, fontFamily: 'Helvetica-Bold', color: brand.colors.primary, marginBottom: 14, marginTop: 180, textAlign: 'center' },
-  backPageText: { fontSize: 11, textAlign: 'center', lineHeight: 1.6, marginBottom: 8, color: '#333' },
+
+  backPage: { paddingTop: 70, paddingBottom: 60, paddingHorizontal: 34, fontFamily: 'Helvetica' },
+
+  // ── Hero: logo + mission ──────────────────────────────────────────
+  hero: { alignItems: 'center', marginBottom: 18 },
+  logo: { width: 62, height: 62, marginBottom: 8 },
+  heroKicker: { fontSize: 9, fontFamily: 'Helvetica-Bold', color: brand.colors.secondary, letterSpacing: 1, marginBottom: 3, textTransform: 'uppercase' },
+  heroName: { fontSize: 17, fontFamily: 'Helvetica-Bold', color: brand.colors.primary, marginBottom: 6, textAlign: 'center' },
+  heroMission: { fontSize: 10, color: '#333', textAlign: 'center', lineHeight: 1.5, maxWidth: 420 },
+  heroContact: { fontSize: 8.5, color: brand.colors.muted, textAlign: 'center', marginTop: 8 },
+
+  // ── Cards row ────────────────────────────────────────────────────
+  cardsRow: { flexDirection: 'row', marginBottom: 16 },
+  card: { flex: 1, marginHorizontal: 4, borderWidth: 1, borderColor: '#e2e8f0', borderRadius: 4, padding: 10 },
+  cardBar: { height: 3, backgroundColor: brand.colors.secondary, marginHorizontal: -10, marginTop: -10, marginBottom: 8, borderTopLeftRadius: 3, borderTopRightRadius: 3 },
+  cardTitle: { fontSize: 10.5, fontFamily: 'Helvetica-Bold', color: brand.colors.primary, marginBottom: 6 },
+  cardText: { fontSize: 8.3, color: '#333', lineHeight: 1.45, marginBottom: 5 },
+  cardQuote: { fontSize: 8, fontFamily: 'Helvetica-BoldOblique', color: brand.colors.primary, lineHeight: 1.4, marginTop: 4 },
+  cardListItem: { flexDirection: 'row', marginBottom: 3 },
+  cardBullet: { width: 8, fontSize: 8.3, color: brand.colors.secondary, fontFamily: 'Helvetica-Bold' },
+  cardListText: { flex: 1, fontSize: 8.3, color: '#333', lineHeight: 1.35 },
+
+  // ── Donation band ────────────────────────────────────────────────
+  donationBand: { backgroundColor: brand.colors.secondary, borderRadius: 4, padding: 16, marginTop: 4 },
+  donationTitle: { fontSize: 13, fontFamily: 'Helvetica-Bold', color: brand.colors.dark, textAlign: 'center', marginBottom: 6 },
+  donationText: { fontSize: 8.5, color: brand.colors.dark, textAlign: 'center', lineHeight: 1.5, marginBottom: 8 },
+  donationImpactRow: { flexDirection: 'row', flexWrap: 'wrap', justifyContent: 'center', marginBottom: 10 },
+  donationImpactItem: { fontSize: 8, color: brand.colors.dark, marginHorizontal: 6, marginBottom: 3 },
+  donationBox: { backgroundColor: '#fff', borderRadius: 3, padding: 10, marginHorizontal: 60 },
+  donationRow: { flexDirection: 'row', justifyContent: 'space-between', marginBottom: 3 },
+  donationLabel: { fontSize: 8.5, color: '#555' },
+  donationValue: { fontSize: 8.5, fontFamily: 'Helvetica-Bold', color: brand.colors.dark },
+  donationThanks: { fontSize: 8, fontStyle: 'italic', color: brand.colors.dark, textAlign: 'center', marginTop: 8 },
 });
 
 export function CoverPage({ title, subtitle }) {
@@ -24,15 +57,106 @@ export function CoverPage({ title, subtitle }) {
   );
 }
 
-export function BackPage({ title }) {
+/**
+ * The back page of every SBA document — a brand trust + conversion page,
+ * not just a contact card. A student who reads to the end should come
+ * away thinking "this academy is serious, I want to join."
+ *
+ * @param {string} title - document title, shown in the running header
+ * @param {'team'|'founder'} authorType - 'founder' for SBA-original,
+ *   hand-authored notes; 'team' (default) for AI-generated resources.
+ */
+export function BackPage({ title, authorType = 'team' }) {
+  const author = authorType === 'founder' ? brand.founder : null;
+
   return (
     <Page size="A4" style={styles.backPage}>
       <Header title={title} />
       <Footer />
-      <Text style={styles.backPageTitle}>About {brand.name}</Text>
-      {brand.about.map((line, i) => (
-        <Text style={styles.backPageText} key={i}>{line}</Text>
-      ))}
+
+      {/* ── Hero ── */}
+      <View style={styles.hero}>
+        <Image src={LOGO_PATH} style={styles.logo} />
+        <Text style={styles.heroKicker}>Impact &amp; Community</Text>
+        <Text style={styles.heroName}>{brand.name}</Text>
+        <Text style={styles.heroMission}>{brand.mission}</Text>
+        <Text style={styles.heroContact}>
+          {brand.website}  •  {brand.emails.join('  •  ')}  •  {brand.phone}
+        </Text>
+      </View>
+
+      {/* ── Three cards: About SBA / About the Author / Join the Community ── */}
+      <View style={styles.cardsRow}>
+        <View style={styles.card}>
+          <View style={styles.cardBar} />
+          <Text style={styles.cardTitle}>About SBA</Text>
+          <Text style={styles.cardText}>
+            {brand.name} (SBA) is an innovative Nigerian EdTech platform helping
+            students prepare for JAMB, WAEC, NECO, Post-UTME, and other exams.
+          </Text>
+          {brand.offerings.slice(0, 5).map((item, i) => (
+            <View style={styles.cardListItem} key={i}>
+              <Text style={styles.cardBullet}>•</Text>
+              <Text style={styles.cardListText}>{item}</Text>
+            </View>
+          ))}
+        </View>
+
+        <View style={styles.card}>
+          <View style={styles.cardBar} />
+          <Text style={styles.cardTitle}>
+            {author ? 'About the Author' : 'About the Content Team'}
+          </Text>
+          {author ? (
+            <>
+              <Text style={[styles.cardText, { fontFamily: 'Helvetica-Bold', marginBottom: 1 }]}>{author.name}</Text>
+              <Text style={[styles.cardText, { color: brand.colors.muted, marginBottom: 5 }]}>{author.title}</Text>
+              <Text style={styles.cardText}>{author.bio}</Text>
+              <Text style={styles.cardQuote}>&ldquo;{author.quote}&rdquo;</Text>
+            </>
+          ) : (
+            <Text style={styles.cardText}>{brand.contentTeam.bio}</Text>
+          )}
+        </View>
+
+        <View style={styles.card}>
+          <View style={styles.cardBar} />
+          <Text style={styles.cardTitle}>Join Our Community</Text>
+          <Text style={styles.cardText}>{brand.community.intro}</Text>
+          {brand.community.benefits.map((item, i) => (
+            <View style={styles.cardListItem} key={i}>
+              <Text style={styles.cardBullet}>•</Text>
+              <Text style={styles.cardListText}>{item}</Text>
+            </View>
+          ))}
+        </View>
+      </View>
+
+      {/* ── Donation / support band ── */}
+      <View style={styles.donationBand}>
+        <Text style={styles.donationTitle}>Support The Mission</Text>
+        <Text style={styles.donationText}>{brand.donation.intro}</Text>
+        <View style={styles.donationImpactRow}>
+          {brand.donation.impact.map((item, i) => (
+            <Text style={styles.donationImpactItem} key={i}>• {item}</Text>
+          ))}
+        </View>
+        <View style={styles.donationBox}>
+          <View style={styles.donationRow}>
+            <Text style={styles.donationLabel}>Bank</Text>
+            <Text style={styles.donationValue}>{brand.donation.bank}</Text>
+          </View>
+          <View style={styles.donationRow}>
+            <Text style={styles.donationLabel}>Account Number</Text>
+            <Text style={styles.donationValue}>{brand.donation.accountNumber}</Text>
+          </View>
+          <View style={styles.donationRow}>
+            <Text style={styles.donationLabel}>Account Name</Text>
+            <Text style={styles.donationValue}>{brand.donation.accountName}</Text>
+          </View>
+        </View>
+        <Text style={styles.donationThanks}>{brand.donation.thanks}</Text>
+      </View>
     </Page>
   );
 }
diff --git a/lib/pdf/StudyNoteDocument.js b/lib/pdf/StudyNoteDocument.js
index 75a1a6d..87a5d1e 100644
--- a/lib/pdf/StudyNoteDocument.js
+++ b/lib/pdf/StudyNoteDocument.js
@@ -65,7 +65,7 @@ function Block({ block, index }) {
   }
 }
 
-export default function StudyNoteDocument({ title, keyword, markdown }) {
+export default function StudyNoteDocument({ title, keyword, markdown, authorType = 'team' }) {
   const blocks = parseMarkdownToBlocks(markdown);
 
   return (
@@ -74,7 +74,7 @@ export default function StudyNoteDocument({ title, keyword, markdown }) {
       <BrandLayout title={title}>
         {blocks.map((b, i) => <Block block={b} index={i} key={i} />)}
       </BrandLayout>
-      <BackPage title={title} />
+      <BackPage title={title} authorType={authorType} />
     </Document>
   );
 }
diff --git a/lib/pdf/brand.js b/lib/pdf/brand.js
index 05f0cdd..eeb4e8c 100644
--- a/lib/pdf/brand.js
+++ b/lib/pdf/brand.js
@@ -6,16 +6,75 @@ const brand = {
   name: 'Shiney Brain Academy',
   website: 'shineybrainacademy.vercel.app',
   phone: '08138082009 / 09053626267',
+  emails: ['florryshine5@gmail.com', 'shineybrainacademy@gmail.com'],
+
   colors: {
-    primary: '#0A1F44',   // navy
-    secondary: '#D4AF37', // gold
+    primary: '#1a73e8',   // bright blue — matches the live site
+    secondary: '#FFCC00', // yellow — matches the live site
     text: '#1a1a1a',
     muted: '#666666',
+    dark: '#0d1b2a',      // for text placed on the yellow background (contrast)
   },
+
+  // Legacy plain-text "about" blurb — kept so any older template that still
+  // imports brand.about doesn't break. The new BackPage below no longer
+  // uses this directly; see mission/offerings/community/founder instead.
   about: [
     'Shiney Brain Academy helps Nigerian students prepare for JAMB, WAEC, NECO, and Post-UTME with quizzes, boss battles, flashcards, and exam-focused study notes.',
     'Visit shineybrainacademy.vercel.app or call 08138082009 / 09053626267 to join our next bootcamp.',
   ],
+
+  mission: 'To make quality education accessible, engaging, and easier to understand for every student.',
+
+  offerings: [
+    'Exam-focused study materials',
+    'Interactive quizzes',
+    'CBT practice',
+    'Boss Battles',
+    'Flashcards',
+    'AI-powered learning tools',
+    'Teacher-guided lessons',
+  ],
+
+  founder: {
+    name: 'Florry Shine',
+    title: 'Founder & CEO, Shiney Brain Academy',
+    bio: 'A passionate educator and technology enthusiast committed to transforming how students learn. Through Shiney Brain Academy, Florry combines teaching experience, technology, and innovation to create a smarter learning environment where students do not just memorize — they understand.',
+    quote: "To build Africa's most impactful AI-powered learning ecosystem, helping millions of students achieve academic success.",
+  },
+
+  // For AI-generated resources where the "About the Author" block should
+  // credit the team rather than one person.
+  contentTeam: {
+    name: 'SBA Academic Team',
+    bio: 'This resource was prepared by the Shiney Brain Academy Academic Team — combining exam-board syllabi, past questions, and AI-assisted research to keep every note accurate and exam-focused.',
+  },
+
+  community: {
+    intro: 'Become part of a growing community of students preparing smarter.',
+    benefits: [
+      'Premium Study Materials',
+      'Practice Questions',
+      'Academic Challenges',
+      'Audio Lessons',
+      'Exam Strategies',
+      'Progress Tracking',
+    ],
+  },
+
+  donation: {
+    intro: "Education changes lives. If you believe in making quality learning resources available to more students, you can support Shiney Brain Academy's mission through donations.",
+    impact: [
+      'Create more free learning resources',
+      'Develop better educational technology',
+      'Provide scholarships for students',
+      'Improve our AI learning tools',
+    ],
+    bank: 'OPay',
+    accountNumber: '8138082009',
+    accountName: 'Igberhi Florry',
+    thanks: 'Thank you for supporting the future of education.',
+  },
 };
 
 export default brand;
diff --git a/lib/podcastPrompt.js b/lib/podcastPrompt.js
index 926cb65..76b15bf 100644
--- a/lib/podcastPrompt.js
+++ b/lib/podcastPrompt.js
@@ -1,3 +1,5 @@
+import { parseJsonFromText } from '@/lib/robustJsonParse';
+
 export function buildPodcastPrompt(post, format = 'teacher_examiner') {
   const { title, content } = post;
 
@@ -95,11 +97,8 @@ Now generate a 15-25 turn podcast episode following the structure above. Make it
 }
 
 export function parseScriptJson(text) {
-  const cleaned = text.replace(/```json|```/g, '').trim();
-  const match = cleaned.match(/\[[\s\S]*\]/);
-  if (!match) return null;
   try {
-    return JSON.parse(match[0]);
+    return parseJsonFromText(text, 'array');
   } catch (e) {
     console.error('JSON parse error:', e.message);
     return null;
diff --git a/lib/robustJsonParse.js b/lib/robustJsonParse.js
new file mode 100644
index 0000000..a7e9fc3
--- /dev/null
+++ b/lib/robustJsonParse.js
@@ -0,0 +1,125 @@
+// lib/robustJsonParse.js
+//
+// Shared JSON parser for AI-generated text (Gemini/Groq/OpenRouter/HuggingFace).
+//
+// THE BUG THIS FIXES:
+// Every engine route used to do `text.match(/\{[\s\S]*\}/)` and then a raw
+// `JSON.parse()` on the result. That works fine when every string value is a
+// single line — but the moment the model returns a multi-sentence paragraph
+// inside a JSON string (very common for "explanation", "content", "summary"
+// fields), the AI's raw text often contains a literal newline character
+// inside that string. Per the JSON spec, a literal newline inside a string
+// is NOT valid — it must be escaped as \n. JSON.parse throws on it, the
+// route's try/catch treats it as a provider failure, and the code moves on
+// to the next provider — even though the model answered correctly. With
+// enough multi-line fields, ALL providers "fail" and you get the generic
+// "All providers failed" error.
+//
+// The old `sanitizeJsonString()` helper made this worse: it stripped control
+// characters EXCEPT \n and \t — i.e. it explicitly preserved the very
+// characters that break parsing.
+//
+// THE FIX:
+// Walk the extracted JSON text character by character. Track whether we're
+// currently inside a string literal (respecting backslash-escaped quotes).
+// Any raw newline/carriage-return/tab found *inside* a string gets escaped
+// to \n / \r / \t. Structural whitespace outside strings is left untouched.
+
+/**
+ * Escape unescaped control characters (newline, carriage return, tab) that
+ * appear inside JSON string literals, without touching structural
+ * whitespace outside strings.
+ */
+function escapeControlCharsInStrings(jsonLike) {
+  let out = '';
+  let inString = false;
+  let escaped = false;
+
+  for (let i = 0; i < jsonLike.length; i++) {
+    const ch = jsonLike[i];
+
+    if (inString) {
+      if (escaped) {
+        out += ch;
+        escaped = false;
+        continue;
+      }
+      if (ch === '\\') {
+        out += ch;
+        escaped = true;
+        continue;
+      }
+      if (ch === '"') {
+        out += ch;
+        inString = false;
+        continue;
+      }
+      if (ch === '\n') {
+        out += '\\n';
+        continue;
+      }
+      if (ch === '\r') {
+        out += '\\r';
+        continue;
+      }
+      if (ch === '\t') {
+        out += '\\t';
+        continue;
+      }
+      // strip other stray control chars that are never valid raw inside a
+      // JSON string (but keep everything else, including unicode text)
+      if (ch.charCodeAt(0) <= 0x1f) {
+        continue;
+      }
+      out += ch;
+      continue;
+    }
+
+    // not currently inside a string
+    if (ch === '"') {
+      inString = true;
+    }
+    out += ch;
+  }
+
+  return out;
+}
+
+/** Best-effort repair for common trailing-comma mistakes models make. */
+function stripTrailingCommas(jsonLike) {
+  return jsonLike.replace(/,\s*([\]}])/g, '$1');
+}
+
+/**
+ * Extract a JSON object or array from raw model output and parse it,
+ * repairing the common "literal newline inside a string" issue first.
+ *
+ * @param {string} text raw text returned by the model
+ * @param {'object'|'array'} shape whether to look for a {...} or [...] block
+ * @returns {any|null} parsed JSON, or null if no candidate block was found
+ * @throws if a candidate block was found but is not valid JSON even after repair
+ */
+function parseJsonFromText(text, shape = 'object') {
+  if (!text) return null;
+
+  const cleaned = text.replace(/```json|```/g, '').trim();
+  const pattern = shape === 'array' ? /\[[\s\S]*\]/ : /\{[\s\S]*\}/;
+  const match = cleaned.match(pattern);
+  if (!match) return null;
+
+  const repaired = escapeControlCharsInStrings(match[0]);
+
+  try {
+    return JSON.parse(repaired);
+  } catch (firstError) {
+    // one more pass: some models also leave trailing commas
+    try {
+      return JSON.parse(stripTrailingCommas(repaired));
+    } catch (secondError) {
+      // surface the original error — it's the more informative one
+      throw firstError;
+    }
+  }
+}
+
+export { parseJsonFromText, escapeControlCharsInStrings };
